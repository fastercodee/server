{
  "private": true,
  "scripts": {
    "dev": "php artisan serve",
    "build": "vite build",
    "format": "prettier --ignore-path .prettierignore -w **/*.php --plugin @prettier/plugin-php"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.19.4",
    "axios": "^1.1.2",
    "laravel-vite-plugin": "^0.7.2",
    "prettier": "^2.8.8",
    "vite": "^4.0.0"
  }
}
